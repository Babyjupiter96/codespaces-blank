name: 🚀 Deploy to Vercel

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache node_modules
        id: cache-modules
        uses: actions/cache@v4
        with:
          path: apps/web/node_modules
          key: modules-${{ hashFiles('apps/web/package-lock.json') }}

      - name: Install dependencies
        if: steps.cache-modules.outputs.cache-hit != 'true'
        working-directory: apps/web
        run: npm ci --no-audit --no-fund --prefer-offline

      # vercel link runs from apps/web to scope the project to that directory.
      # vercel pull/build/deploy run from repo root so rootDirectory=apps/web
      # in the project settings resolves correctly without double-pathing.
      - name: Link project
        working-directory: apps/web
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: npx vercel@54.4.1 link --project graystone-web --yes --token=$VERCEL_TOKEN

      - name: Pull & Build
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          npx vercel@54.4.1 pull --yes --environment=production --token=$VERCEL_TOKEN
          npx vercel@54.4.1 build --prod --token=$VERCEL_TOKEN
          echo "=== .vercel/output at repo root ==="
          ls -la .vercel/output/ 2>/dev/null || echo "NOT FOUND at repo root"
          echo "=== .vercel/output inside apps/web ==="
          ls -la apps/web/.vercel/output/ 2>/dev/null || echo "NOT FOUND inside apps/web"
          echo "=== .vercel/output/config.json (repo root) ==="
          cat .vercel/output/config.json 2>/dev/null || echo "NOT FOUND"

      # ── Preview (pull requests) ───────────────────────────────────────────
      - name: Deploy Preview
        if: github.event_name == 'pull_request'
        id: preview
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          URL=$(npx vercel@54.4.1 deploy --prebuilt --token=$VERCEL_TOKEN 2>&1 | tail -1)
          echo "url=$URL" >> $GITHUB_OUTPUT
          echo "Preview: $URL"

      - name: Comment Preview URL on PR
        if: github.event_name == 'pull_request' && steps.preview.outputs.url != ''
        uses: actions/github-script@v7
        with:
          script: |
            const url = '${{ steps.preview.outputs.url }}';
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🚀 Preview\n\n**URL:** ${url}\n**Commit:** \`${context.sha.slice(0,7)}\``,
            });

      # ── Production (push to main) ─────────────────────────────────────────
      - name: Deploy Production
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          OUTPUT=$(npx vercel@54.4.1 deploy --prebuilt --prod --token=$VERCEL_TOKEN 2>&1)
          echo "$OUTPUT"
          URL=$(echo "$OUTPUT" | grep -Eo 'https://[a-zA-Z0-9._-]+\.vercel\.app' | tail -1)
          echo ""
          echo "=========================================="
          echo "LIVE URL: $URL"
          echo "=========================================="
